## Linear

<!-- CMD-XX in title. Fixes = auto-Done on merge. Ref = stays In Review until manual QA → Done + Verified. -->

**Expected status flow:** In Progress (draft PR) → In Review (opened) → Ready to Merge (CI green) → Done after verify.

Fixes CMD-

## Summary



## Test plan

- [ ] `dotnet test tests/ChatGPTWrapper.ApiDiagnostics --filter "Category=Unit"`
- [ ] Manual QA (if **Needs Manual QA** — use `Ref CMD-XX` above; move to Done + Verified after play test)
