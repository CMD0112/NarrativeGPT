<Window x:Class="ChatGPTWrapper.PhraseHighlightsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ChatGPTWrapper"
        Title="Phrase highlights"
        Height="580"
        Width="680"
        MinHeight="480"
        MinWidth="560"
        Background="{StaticResource DialogBgBrush}"
        Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <local:PhraseHighlightsEditorControl x:Name="EditorControl" Grid.Row="0"/>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="OK"
                    Style="{StaticResource PrimaryButtonStyle}"
                    MinWidth="88"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    Click="OkButton_Click"/>
            <Button Content="Cancel"
                    Padding="16,6"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
