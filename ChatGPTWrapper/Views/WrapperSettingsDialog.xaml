<Window x:Class="ChatGPTWrapper.Views.WrapperSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Wrapper settings"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BgBaseBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Adventures folder"
                   FontWeight="SemiBold" Margin="0,0,0,6"/>
        <TextBlock Grid.Row="1"
                   TextWrapping="Wrap"
                   Foreground="{StaticResource TextMutedBrush}"
                   FontSize="11"
                   Margin="0,0,0,8"
                   Text="Each adventure is stored as {folder}/{adventure-id}/. Config and browser data stay under %LocalAppData%\ChatGPTWrapper\."/>

        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="AdventuresPathBox" Padding="6,4" MinWidth="320"
                     ToolTip="Leave empty to use the default adventures folder"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="8,0,0,0">
                <Button Content="Browse…" Padding="10,4" Margin="0,0,6,0" Click="Browse_Click"/>
                <Button Content="Default" Padding="10,4" Click="Default_Click"/>
            </StackPanel>
        </Grid>

        <TextBlock x:Name="DefaultPathLine" Grid.Row="3"
                   TextWrapping="Wrap"
                   Foreground="{StaticResource TextMutedBrush}"
                   FontSize="11"
                   Margin="0,0,0,12"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Style="{StaticResource PrimaryButtonStyle}"
                    Padding="12,6" Margin="0,0,8,0" Click="Save_Click" IsDefault="True"/>
            <Button Content="Cancel" Padding="12,6" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
