<Window x:Class="ChatGPTWrapper.Views.EntityRetireDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Retire entity"
        Height="280"
        Width="480"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgBaseBrush}"
        Foreground="{StaticResource TextPrimaryBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="RetireSummaryLine" TextWrapping="Wrap" Margin="0,0,0,12"/>
        <CheckBox x:Name="AliasOnlyCheck"
                  Grid.Row="1"
                  Content="Keep name as alias only in lexicon (soft retire)"
                  Margin="0,0,0,8"/>
        <TextBlock Grid.Row="2"
                   Text="Retiring adds a “retired” tag and refreshes source export. The entity remains in JSON for history."
                   TextWrapping="Wrap"
                   Foreground="{StaticResource TextMutedBrush}"
                   FontSize="11"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Padding="12,6" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Retire" Padding="12,6" IsDefault="True" Click="Retire_Click"/>
        </StackPanel>
    </Grid>
</Window>
