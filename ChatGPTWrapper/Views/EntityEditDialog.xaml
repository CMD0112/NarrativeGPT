<Window x:Class="ChatGPTWrapper.Views.EntityEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit entity"
        Height="440"
        Width="480"
        MinHeight="400"
        MinWidth="440"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource DialogBgBrush}"
        Foreground="{StaticResource TextBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Name" Margin="0,0,0,4"/>
        <TextBox x:Name="NameBox" Grid.Row="1" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="2" Text="Role / status" Margin="0,0,0,4"/>
        <TextBox x:Name="RoleBox" Grid.Row="3" Margin="0,0,0,10"/>
        <TextBlock Grid.Row="4" Text="Description" Margin="0,0,0,4"/>
        <TextBox x:Name="DescriptionBox" Grid.Row="5" MinHeight="120"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,10"/>
        <CheckBox x:Name="PinnedCheck" Grid.Row="6" Content="Pinned (include in packets when matched)"
                  Margin="0,0,0,4"/>
        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Padding="12,6" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Save" Style="{StaticResource PrimaryButtonStyle}" MinWidth="88" IsDefault="True" Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Window>
