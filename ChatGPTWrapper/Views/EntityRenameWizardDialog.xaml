<Window x:Class="ChatGPTWrapper.Views.EntityRenameWizardDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename entity"
        Height="620"
        Width="760"
        MinHeight="480"
        MinWidth="640"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgBaseBrush}"
        Foreground="{StaticResource TextPrimaryBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="RenameSummaryLine" TextWrapping="Wrap" Margin="0,0,0,8"/>
        <DataGrid x:Name="MentionsGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Location" Binding="{Binding File}" Width="120" IsReadOnly="True"/>
                <DataGridTextColumn Header="Term" Binding="{Binding MatchedTerm}" Width="80" IsReadOnly="True"/>
                <DataGridTextColumn Header="Context" Binding="{Binding Snippet}" Width="*" IsReadOnly="True"/>
                <DataGridComboBoxColumn Header="Action" Width="100"
                                      SelectedItemBinding="{Binding Action, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </DataGrid.Columns>
        </DataGrid>
        <TextBox x:Name="DiffPreviewBox"
                 Grid.Row="2"
                 Height="120"
                 Margin="0,8,0,0"
                 IsReadOnly="True"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="10"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cancel" Padding="12,6" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Apply rename" Padding="12,6" IsDefault="True" Click="Apply_Click"/>
        </StackPanel>
    </Grid>
</Window>
