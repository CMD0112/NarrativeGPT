<Window x:Class="ChatGPTWrapper.Views.AdventureRenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename adventure"
        Height="180"
        Width="440"
        MinHeight="160"
        MinWidth="360"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource DialogBgBrush}"
        Foreground="{StaticResource TextBrush}"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Title" Margin="0,0,0,4"/>
        <TextBox x:Name="TitleBox" Grid.Row="1" Margin="0,0,0,16"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Margin="0,0,8,0" Padding="14,6" IsCancel="True"/>
            <Button Content="Rename" Style="{StaticResource PrimaryButtonStyle}" MinWidth="88" IsDefault="True" Click="Rename_Click"/>
        </StackPanel>
    </Grid>
</Window>
