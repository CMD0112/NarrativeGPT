<UserControl x:Class="ChatGPTWrapper.Views.EntityInternalStateFormHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,4,0">
            <TextBlock x:Name="SummaryText"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource TextMutedBrush}"
                       FontSize="11"
                       Margin="0,0,0,10"
                       Visibility="Collapsed"/>
            <TextBlock Text="Internal state tracks mood, injuries, progress, and other mutable play facts — separate from canon description."
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource TextMutedBrush}"
                       FontSize="10"
                       Margin="0,0,0,10"/>
            <CheckBox x:Name="ShowEmptySectionsCheck"
                      Content="Show empty sections"
                      Margin="0,0,0,10"
                      Checked="ShowEmptySectionsCheck_Changed"
                      Unchecked="ShowEmptySectionsCheck_Changed"/>
            <StackPanel x:Name="LifecycleActionsPanel" Orientation="Horizontal" Margin="0,0,0,10" Visibility="Collapsed"/>
            <StackPanel x:Name="SectionsPanel"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
